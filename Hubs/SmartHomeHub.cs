using Microsoft.AspNetCore.SignalR;

namespace SmartHomeManager.Hubs
{
    public class SmartHomeHub : Hub
    {
        // Intentionally empty for now — server will push "UpdateUI" events to clients.
    }
}